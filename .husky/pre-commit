node -e "const v=+process.versions.node.split('.')[0];if(v>=20){const{spawnSync}=require('child_process');const r=spawnSync('npx',['lint-staged'],{stdio:'inherit',shell:true});process.exit(r.status??1)}"
